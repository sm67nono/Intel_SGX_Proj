package dsp.core.arithmetic.Tokenizer;

public class Testing_App {
	
	/*public static void main(String[] args) {						
		//Test Example				
		try {
			Core_Tokenizer create_tokenizer = new Core_Tokenizer();
			Arithmetic_Token_List apg = new Arithmetic_Token_List();
			create_tokenizer.setT_Info(apg.setParserGrammerArith());
			
			//Input
			create_tokenizer.tokenize_the_input(" sin(x) * (1 + var_12) ");

			  for (Tokens get_token : create_tokenizer.getTokens()) {
			    System.out.println("" + get_token.token_ID + " " + get_token.seq);
			  }
			}
			catch (ParserException e) {
			  System.out.println(e.getMessage());
			}
	}*/

}
